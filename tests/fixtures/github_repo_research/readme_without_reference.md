# Handy CLI Tool

A small command-line utility for renaming files in bulk.

## Installation

```bash
pip install handy-cli
```

## Usage

```bash
handy-cli --pattern "*.txt" --rename
```

No associated paper or publication -- just a practical tool.

# Python
__pycache__/
.pytest_cache/
*.py[cod]
*.egg-info/
venv/
.venv/
build/
dist/

# Werkzeug-Caches und Testberichte — reine Laufzeit-Artefakte
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
coverage.xml
htmlcov/
*.log

# Plugin local config (user-specific)
*.local.md

# Harness-Anweisungen der Wurzel bleiben lokal — verbindlich fuer alle ist
# AGENTS.md. `evals/CLAUDE.md` und `scripts/bootstrap/CLAUDE.md` sind
# funktionale Repo-Dateien und darum ausdruecklich nicht gemeint.
/CLAUDE.md

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Claude Code: flowkit-Konfiguration ist versioniert (settings, hooks, workflow-config),
# Nutzer-Spezifisches (settings.local.json, worktrees/, agents/) bleibt lokal (#343)
.claude/*
!.claude/settings.json
!.claude/hooks/
!.claude/workflow.config.json
!.claude/flowkit-version

# flowkit-Laufzeitdaten (Drafts, Runner-State)
.flowkit/

# Runtime data (stored in ~/.academic-research/)
sessions/
pdfs/

# Vault-Datenbank — Forschungs-PII, nie ins Repo (Issue #190, CWE-538)
*.db
vault.db

# Secrets
.env
.env.local

# Brainstorming-Artefakte (persoenliche Arbeitsprodukte, nicht Release-Inhalt).
# Das Verzeichnis ist mit #641 vollstaendig aus der Versionierung genommen; was
# noch darin liegt, ist lokal und bleibt lokal.
docs/superpowers/

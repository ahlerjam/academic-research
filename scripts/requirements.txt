anthropic>=0.40
httpx>=0.28.1
pypdf>=4.0.0
pyyaml>=6.0.3
# requests>=2.32.4: CVE-2024-35195 (cert verification bypass), CVE-2024-47081 (.netrc credential leak)
requests>=2.32.4
# lxml>=5.2.0: CVE-2022-2309 (NULL pointer deref)
lxml>=5.2.0
# Barcode-Generierung fuer /academic-research:pickup (Skill: barcode_utils)
python-barcode[images]>=0.16.1
# academic-vault MCP-Server (Vault): FastMCP-SDK + Vektor-Suche-Extension
mcp>=1.0
sqlite-vec>=0.1.0
# strikte csl_json-Validierung in vault.add_paper (Issue #213)
jsonschema>=4.0
# optional — nur fuer Zotero-Import (Skill: zotero-import)
pyzotero>=1.5
# optional — lokale Embedding-Pipeline fuer die Vektor-Suche (Issue #372).
# Bewusst NICHT aktiviert: sentence-transformers zieht Torch (~2,5 GB) nach.
# Ohne dieses Paket laeuft der Vault unveraendert im FTS5-only-Modus weiter;
# mit ihm nutzt vault.search(rerank=True) echte Vektortreffer
# (intfloat/multilingual-e5-small, 384d, MIT). Aktivieren via:
#   pip install "sentence-transformers>=3.0"
# sentence-transformers>=3.0
